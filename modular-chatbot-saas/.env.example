# Postgres (used for vector DB + app data)
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=modular_chatbot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Redis (for queues and ephemeral cache)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Backend API (for legacy backend server)
PORT=3000

# Frontend Environment Variables
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
